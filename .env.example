# Опционально — только для локальной разработки без Docker.
# На сервере .env НЕ нужен: docker compose up -d --build

DATABASE_URL=postgres://marka:marka@localhost:5432/marka?sslmode=disable
PORT=3001
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin_password
MINIO_BUCKET=marka
MINIO_USE_SSL=false

# API_URL=http://localhost:3001
# NEXT_PUBLIC_API_URL=http://localhost:3001
