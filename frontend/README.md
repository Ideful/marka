# frontend

Публичный сайт салона (Next.js App Router).

```bash
npm install
npm run dev      # http://localhost:3000
```

Переменные: `API_URL` или `NEXT_PUBLIC_API_URL` (по умолчанию `http://localhost:3001`).
