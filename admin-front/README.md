# admin-front

Админ-панель: специалисты, загрузка фото в MinIO.

```bash
npm install
npm run dev      # http://localhost:5173
```

Запросы к API идут на `/api` (прокси Vite → `http://localhost:3001`). Нужен запущенный `make dev-backend`.
