{
  "name": "marka",
  "private": true,
  "description": "Монорепо: frontend, admin-front, backend",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin-front",
    "install:all": "npm install --prefix frontend && npm install --prefix admin-front",
    "build:frontend": "npm run build --prefix frontend",
    "build:admin": "npm run build --prefix admin-front"
  }
}
